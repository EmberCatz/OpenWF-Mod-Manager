# Node
node_modules/
dist/
dist-ssr/

# Tauri / Rust
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/Cargo.lock
apps/desktop/src-tauri/gen/schemas/

# Cloudflare
.wrangler/
apps/api/.dev.vars

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code (keeps local settings, session state, and conversation
# history out of this public repo)W
.claude/
CLAUDE.local.md
**/.claude/settings.local.json

# Private operational notes not meant for the public repo
ADMIN.local.md

# Security audit docs — these contain working exploit PoCs (exact curl
# commands, attack chains). Fine to keep locally for reference, never
# meant for a public repo, especially while any finding might still be
# undeployed in production.
docs/security-audit-2026-09.md
docs/redteam-audit-2026-09.md

# Logs
*.log
npm-debug.log*
